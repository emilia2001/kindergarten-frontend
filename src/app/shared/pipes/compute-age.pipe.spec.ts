import { ComputeAgePipe } from './compute-age.pipe';

describe('ComputeAgePipe', () => {
  it('create an instance', () => {
    const pipe = new ComputeAgePipe();
    expect(pipe).toBeTruthy();
  });
});
