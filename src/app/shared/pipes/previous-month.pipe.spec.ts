import { PreviousMonthPipe } from './previous-month.pipe';

describe('PreviousMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviousMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
