import { FilterChildrenPipe } from './filter-children.pipe';

describe('FilterChildrenPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChildrenPipe();
    expect(pipe).toBeTruthy();
  });
});
