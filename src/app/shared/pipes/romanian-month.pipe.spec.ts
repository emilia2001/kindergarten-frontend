import { RomanianMonthPipe } from './romanian-month.pipe';

describe('RomanianMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new RomanianMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
