import { GroupTypePipe } from './group-type.pipe';

describe('GroupTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTypePipe();
    expect(pipe).toBeTruthy();
  });
});
