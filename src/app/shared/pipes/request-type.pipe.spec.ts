import { RequestTypePipe } from './request-type.pipe';

describe('RequestTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RequestTypePipe();
    expect(pipe).toBeTruthy();
  });
});
